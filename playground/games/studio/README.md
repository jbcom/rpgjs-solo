# Studio

Official RPGJS Studio integration with maps, modules, and runtime editing
workflows.

## Run

From the repository root:

```bash
pnpm playground
```

Then select **Studio** in the launcher at `http://localhost:5174`.

Run only this game:

```bash
pnpm --dir playground dev:studio
```

Direct URL: `http://localhost:5182`.
