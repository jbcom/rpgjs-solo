# Action Battle

Real-time RPG combat with attacks, rewards, player stats, and battle-oriented UI.

## Run

From the repository root:

```bash
pnpm playground
```

Then select **Action Battle** in the launcher at `http://localhost:5174`.

Run only this game:

```bash
pnpm --dir playground dev:action-battle
```

Direct URL: `http://localhost:5180`.
