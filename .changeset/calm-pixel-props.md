---
'@jbcom/rpgjs-solo-renderer': patch
---

Add reactive game-owned entity visibility so hidden authored props remain
invisible instead of rendering fallback circles and can appear later without
replacing their spritesheet identity.
