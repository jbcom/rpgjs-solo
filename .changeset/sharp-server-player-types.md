---
"@rpgjs/server": patch
---

Align player, event, and map hooks with their runtime arguments, add the missing player load/save hooks, and expose stricter types for snapshots, save/load results, variables, GUI results, skills, states, classes, combat, and custom server events. Preserve public `@rpgjs/*` imports in generated declarations.
