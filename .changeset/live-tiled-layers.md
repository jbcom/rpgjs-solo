---
"@jbcom/rpgjs-solo-renderer": patch
---

Replace complete Tiled tile layers at runtime through immutable, revisioned rendered maps so story-driven gates and terrain changes stay visually aligned with authoritative collision.
