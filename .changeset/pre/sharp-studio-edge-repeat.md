---
"@rpgjs/studio": patch
---

Keep Studio map elements constrained to their repeat axis when a placed element only overrides one dimension, and render repeat/edge-repeat segments by tiling the repeated slice instead of scaling every segment across the resized axis.
