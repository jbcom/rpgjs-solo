---
"@rpgjs/physic": patch
---

Refresh the physics package's development runner to the current compatible
`tsx` release used by the Node 24 workspace toolchain, with a deterministic
package-script smoke contract for the real benchmark import graph.
