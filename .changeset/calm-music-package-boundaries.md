---
"@rpgjs/client": patch
"@rpgjs/testing": patch
---

Parse audio source suffixes without a backtracking regular expression and keep
Signe imports as portable package specifiers in packed client and testing
artifacts instead of embedding workspace-specific pnpm paths.
